# this is an auto generated file. This will be overwritten
mutation UploadFile($input: UploadInput!, $file: S3ObjectInput) {
  uploadFile(input: $input, file: $file) {
    pk
    fileName
    createdAt
    status
    file {
      bucket
      key
      region
    }
  }
}
mutation BroadcastNotification($title: String!, $body: String) {
  broadcastNotification(title: $title, body: $body) {
    success
    message
  }
}
mutation SubscribeBeneficiary($citizenId: String!) {
  subscribeBeneficiary(citizenId: $citizenId) {
    pk
    citizenId
    createdAt
  }
}
mutation UnsubscribeBeneficiary($citizenId: String!) {
  unsubscribeBeneficiary(citizenId: $citizenId) {
    pk
    citizenId
    createdAt
  }
}
mutation UpdateEndpointAttributes(
  $endpointId: String!
  $action: EndpointAction!
) {
  updateEndpointAttributes(endpointId: $endpointId, action: $action) {
    success
    message
  }
}
