# this is an auto generated file. This will be overwritten
mutation SubscribeBeneficiary($citizenId: String!) {
  subscribeBeneficiary(citizenId: $citizenId) {
    id
    pk
    citizenId
    createdAt
    hasNewDeposits
    aws_ds
  }
}
mutation UnsubscribeBeneficiary($citizenId: String!) {
  unsubscribeBeneficiary(citizenId: $citizenId) {
    id
    pk
    citizenId
    createdAt
    hasNewDeposits
    aws_ds
  }
}
mutation UpdateEndpointAttributes(
  $endpointId: String!
  $action: EndpointAction!
) {
  updateEndpointAttributes(endpointId: $endpointId, action: $action) {
    success
    message
  }
}
mutation UpdateSubscription(
  $username: String!
  $citizenId: String!
  $hasNewDeposits: Boolean!
) {
  updateSubscription(
    username: $username
    citizenId: $citizenId
    hasNewDeposits: $hasNewDeposits
  ) {
    id
    pk
    citizenId
    createdAt
    hasNewDeposits
    aws_ds
  }
}
mutation UpdateNewDepositsState(
  $citizenId: String!
  $hasNewDeposits: Boolean!
) {
  updateNewDepositsState(
    citizenId: $citizenId
    hasNewDeposits: $hasNewDeposits
  ) {
    id
    pk
    citizenId
    createdAt
    hasNewDeposits
    aws_ds
  }
}
